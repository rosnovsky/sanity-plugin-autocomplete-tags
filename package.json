{
  "name": "turborepo-basic",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "docs",
    "apps/*",
    "ui",
    "config",
    "tsconfig",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build --parallel"
  },
  "devDependencies": {
    "prettier": "^2.5.1",
    "turbo": "^1.6.3"
  }
}
